//Q.31 No Users: Add an if test to Exercise 28 to make sure the list of users is not empty.
let array: string[] = ['admin','sharjeel','ramzan','dalilah','maaz'];
if(array.length>0){
    array=[];
    console.log('We need to find some users!');
}
else{
    console.log('We need to find some users!');
};
